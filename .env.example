# Optional — only if you override MCP auth with an API key instead of OAuth.
# Get a key at https://docs.sixtyfour.ai/get-api-key
# Default plugin auth is OAuth (no env var required).
SIXTYFOUR_API_KEY=
